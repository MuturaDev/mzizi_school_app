<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">



    <item android:id="@+id/find_help_ID"
        android:icon="@drawable/support_help_icon"
        android:title="Refresh"
        android:tooltipText="Tap to get help"
        android:alphabeticShortcut="r"
        android:orderInCategory="1"
        app:showAsAction="always"
        />

    <item android:id="@+id/upload_btn"
        android:icon="@drawable/upload_to_cloud_icon"
        android:title="Refresh"
        android:tooltipText="Tap to sync data"
        android:alphabeticShortcut="r"
        android:orderInCategory="1"
        app:showAsAction="always"
        />

    <item android:id="@+id/refresh_btn"
        android:icon="@drawable/download_from_cloud_icon"
        android:title="Refresh"
        android:tooltipText="Tap to sync data"
        android:alphabeticShortcut="r"
        android:orderInCategory="1"
        app:showAsAction="always"
        />
    <item
        android:id="@+id/logout_id"
        android:title="Logout"
        android:orderInCategory="1"
        app:showAsAction="never"

        />


</menu>
